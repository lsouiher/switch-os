services:
  db:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: switchos
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
